import React,{useState} from 'react';import {Alert,ScrollView,StyleSheet,Text,View} from 'react-native';import {router} from 'expo-router';import {SafeAreaView} from 'react-native-safe-area-context';import {Button,Field,H1,Muted} from '@/src/components/UI';import {useTheme} from '@/src/context/ThemeContext';import {setSetting} from '@/src/db/database';
export default function Setup(){const {theme}=useTheme();const [name,setName]=useState(''),[owner,setOwner]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[address,setAddress]=useState(''),[currency,setCurrency]=useState('ZAR'),[tax,setTax]=useState('15');const save=async()=>{await setSetting('business_profile',JSON.stringify({name,owner,phone,email,address}));await setSetting('currency',currency||'ZAR');await setSetting('tax_rate',tax||'0');await setSetting('onboarding_complete','1');router.replace('/(tabs)')};return <SafeAreaView style={{flex:1,backgroundColor:theme.bg}}><ScrollView contentContainerStyle={s.c}><H1>Set up your business</H1><Muted>Just the essentials. You can change everything later in Settings.</Muted><Field label="Business name" value={name} onChangeText={setName} placeholder="Your Business Name"/><Field label="Owner / contact" value={owner} onChangeText={setOwner}/><Field label="Phone" value={phone} onChangeText={setPhone} keyboardType="phone-pad"/><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address"/><Field label="Address" value={address} onChangeText={setAddress} multiline/><View style={{flexDirection:'row',gap:12}}><View style={{flex:1}}><Field label="Currency" value={currency} onChangeText={setCurrency}/></View><View style={{flex:1}}><Field label="Default tax %" value={tax} onChangeText={setTax} keyboardType="decimal-pad"/></View></View><Button title="Set Up My Business" onPress={save}/><Button title="Skip for now" variant="secondary" onPress={async()=>{await setSetting('onboarding_complete','1');router.replace('/(tabs)')}}/></ScrollView></SafeAreaView>}
const s=StyleSheet.create({c:{padding:24,gap:18,paddingBottom:40}})
