import React from 'react';import {Pressable,Text,View} from 'react-native';import {router} from 'expo-router';import {SafeAreaView} from 'react-native-safe-area-context';import {Card,Muted} from '@/src/components/UI';import {Header} from '@/src/components/Header';import {useTheme} from '@/src/context/ThemeContext';import {setSetting} from '@/src/db/database';
export default function Appearance(){const {theme,mode,setMode}=useTheme();return <SafeAreaView style={{flex:1,backgroundColor:theme.bg}}><Header title="Appearance" onBack={()=>router.back()}/><View style={{padding:18,gap:12}}><Muted>Light and dark modes are both designed for Unifill. System follows your Android setting.</Muted>{(['system','light','dark'] as const).map(x=><Pressable key={x} onPress={async()=>{setMode(x);await setSetting('appearance',x)}}><Card style={{flexDirection:'row',justifyContent:'space-between'}}><Text style={{color:theme.text,fontWeight:'900',textTransform:'capitalize'}}>{x}</Text><Text style={{color:mode===x?theme.primary:theme.muted,fontWeight:'900'}}>{mode===x?'Selected':''}</Text></Card></Pressable>)}</View></SafeAreaView>}
